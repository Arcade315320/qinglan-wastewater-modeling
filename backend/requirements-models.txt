-r requirements.txt

# QSDsan 1.5.3 currently needs the scientific stack on Python 3.12.
# NumPy 2.x can break binary dependencies in older pandas/pyarrow builds.
qsdsan==1.5.3
numpy>=1.26,<2
pandas>=2.2,<3
scipy>=1.15,<1.16
